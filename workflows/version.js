// Shown small on the menu screen. VERSION is edited by hand; BUILD is stamped automatically
// by .github/workflows/stamp.yml on every push to main (it commits the short commit id + date).
export const VERSION = '12.4';
export const BUILD = 'dev';
