diff --git a/README.md b/README.md
index a16918d..d5b6f5a 100644
--- a/README.md
+++ b/README.md
@@ -9,7 +9,8 @@ npm test             # physics, track, barrel and derby tests (Node 20+, no depe
 ```
 
 Try `?track=hills` (or `speedway`, `kidney`, `technical`, `overpass`, `random`) to jump straight in, and `&edit=1`
-to open the editor. `random` builds a fresh circuit every time - hills, jumps, banks, barrels and chickens.
+to open the editor. `random` builds a fresh circuit every time - hills, jumps, banks, barrels and chickens. The
+**Play Now** button on the menu does the same thing with one click, no URL needed.
 
 ## Controls
 
diff --git a/css/style.css b/css/style.css
index 0554daa..c4c63a3 100644
--- a/css/style.css
+++ b/css/style.css
@@ -44,6 +44,9 @@ body:not(.mode-menu) #menu { display: none; }
   clip-path: polygon(10px 0, 100% 0, calc(100% - 10px) 100%, 0 100%); }
 .btn:hover { background: #ffc44d; }
 .btn.ghost { background: transparent; color: var(--peach); box-shadow: inset 0 0 0 2px rgba(246,217,176,.45); }
+.btn-play { display: block; font-size: 28px; padding: 16px 36px; margin: 10px 0 6px; box-shadow: 0 6px 0 #8a5200, 0 10px 18px rgba(0,0,0,.35); transition: transform .08s, box-shadow .08s; }
+.btn-play:hover { transform: translateY(-2px); box-shadow: 0 8px 0 #8a5200, 0 14px 20px rgba(0,0,0,.4); }
+.btn-play:active { transform: translateY(2px); box-shadow: 0 3px 0 #8a5200, 0 6px 12px rgba(0,0,0,.35); }
 .options { margin: 4px 0 12px; color: var(--peach); }
 .options summary { cursor: pointer; font-family: var(--cond); font-weight: 600; font-size: 18px; color: var(--amber); }
 .opt-grid { display: grid; gap: 8px; padding: 10px 0; font-size: 15px; }
diff --git a/index.html b/index.html
index ee8e621..a4818e1 100644
--- a/index.html
+++ b/index.html
@@ -99,8 +99,8 @@
 <section id="menu">
   <div class="menu-inner">
     <h1><span>Car</span> <span>Racer</span></h1>
+    <button id="btn-play-now" class="btn btn-play">Play Now</button>
     <div id="version">v12.4</div>
-    <div id="version-note" class="muted small"></div>
     <p class="lede">Heavy steel, wide roads, and a handbrake. Pick a circuit, or draw your own.</p>
 
     <ul id="track-list" class="track-list"></ul>
@@ -210,9 +210,8 @@
 <div id="fatal" hidden></div>
 
 <script type="module">
-  import { VERSION, BUILD, NOTE } from './src/version.js';
+  import { VERSION, BUILD } from './src/version.js';
   document.getElementById('version').textContent = 'v' + VERSION + (BUILD && BUILD !== 'dev' ? ' \u00b7 ' + BUILD : '');
-  if (NOTE) document.getElementById('version-note').textContent = NOTE;
 </script>
 <script type="module" src="src/main.js"></script>
 </body>
diff --git a/src/main.js b/src/main.js
index 202a40d..e62cac9 100644
--- a/src/main.js
+++ b/src/main.js
@@ -582,6 +582,7 @@ function bindMenu() {
     sw.appendChild(b);
   });
   $('btn-new').onclick = () => { const d = makeTemplate('kidney'); d.name = 'My track'; openEditor(d); };
+  $('btn-play-now').onclick = () => startDriving(makeRandomTrack());
   $('import-file').onchange = async (e) => {
     const f = e.target.files[0]; e.target.value = ''; if (!f) return;
     try {
diff --git a/src/version.js b/src/version.js
index 1f628f3..2849d61 100644
--- a/src/version.js
+++ b/src/version.js
@@ -1,8 +1,9 @@
 // Shown small on the menu screen.
-// VERSION and NOTE are edited by hand together, every time a change lands: bump VERSION
-// and set NOTE to a short line describing what changed, e.g. 'AI cars now line up on the grid'.
+// VERSION is edited by hand, every time a change lands - bump it and set NOTE to a short line describing
+// what changed, e.g. 'AI cars now line up on the grid'. NOTE isn't shown on screen any more (removed from
+// the menu), but keep filling it in anyway: it's a handy one-line changelog to glance back through.
 // BUILD is stamped automatically by .github/workflows/stamp.yml on every push to main
 // (it commits the short commit id + date, so you can always tell exactly what's live).
-export const VERSION = '13.8';
-export const NOTE = 'Splatting a chicken near you now scores 10 points';
-export const BUILD = 'afb28c2 26 Sep 16:03';
+export const VERSION = '13.9';
+export const NOTE = 'A big Play Now button by the logo jumps straight into a random track';
+export const BUILD = 'dev';
